export type ReactionModalReactors = {
  reactionType: string;
  loading: boolean;
  page: boolean;
  error: string;
  totalRows: number;
  totalPages: number;
};

export type ReactionModalHeader = {
  reactionType: string;
  count: number;
};
